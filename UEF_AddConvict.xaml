<UserControl x:Class="Social_Blade_Dashboard.UEF_AddConvict" 
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             d:DesignWidth="450" d:DesignHeight="900">

    <UserControl.Resources>
        <Style x:Key="RoundedTextBoxStyle" TargetType="TextBox">
            <Setter Property="Height" Value="38"/>
            <Setter Property="Background" Value="White"/>
            <Setter Property="BorderBrush" Value="#D1D5DB"/>
            <Setter Property="BorderThickness" Value="2"/>
            <Setter Property="Padding" Value="12,8"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
            <Setter Property="Foreground" Value="Black"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="TextBox">
                        <Border x:Name="border"
                                Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="6"
                                SnapsToDevicePixels="True">
                            <ScrollViewer x:Name="PART_ContentHost"
                                          Focusable="false"
                                          HorizontalScrollBarVisibility="Hidden"
                                          VerticalScrollBarVisibility="Hidden"
                                          Margin="{TemplateBinding Padding}"
                                          VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsEnabled" Value="false">
                                <Setter Property="Opacity" TargetName="border" Value="0.56"/>
                            </Trigger>
                            <Trigger Property="IsMouseOver" Value="true">
                                <Setter Property="BorderBrush" TargetName="border" Value="#9CA3AF"/>
                            </Trigger>
                            <Trigger Property="IsKeyboardFocused" Value="true">
                                <Setter Property="BorderBrush" TargetName="border" Value="#10B981"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="ModernScrollBarThumb" TargetType="{x:Type Thumb}">
            <Setter Property="OverridesDefaultStyle" Value="true"/>
            <Setter Property="IsTabStop" Value="false"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="{x:Type Thumb}">
                        <Rectangle x:Name="rectangle" 
                                   Fill="Transparent" 
                                   Width="1" 
                                   Height="{TemplateBinding Height}"
                                   RadiusX="4" 
                                   RadiusY="4"
                                   Opacity="0.7">
                            <Rectangle.RenderTransform>
                                <TranslateTransform X="0"/>
                            </Rectangle.RenderTransform>
                        </Rectangle>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Trigger.EnterActions>
                                    <BeginStoryboard>
                                        <Storyboard>
                                            <DoubleAnimation Storyboard.TargetName="rectangle" 
                                                             Storyboard.TargetProperty="Opacity" 
                                                             To="1.0" Duration="0:0:0.2"/>
                                            <DoubleAnimation Storyboard.TargetName="rectangle" 
                                                             Storyboard.TargetProperty="Width" 
                                                             To="10" Duration="0:0:0.2"/>
                                        </Storyboard>
                                    </BeginStoryboard>
                                </Trigger.EnterActions>
                                <Trigger.ExitActions>
                                    <BeginStoryboard>
                                        <Storyboard>
                                            <DoubleAnimation Storyboard.TargetName="rectangle" 
                                                             Storyboard.TargetProperty="Opacity" 
                                                             To="0.7" Duration="0:0:0.2"/>
                                            <DoubleAnimation Storyboard.TargetName="rectangle" 
                                                             Storyboard.TargetProperty="Width" 
                                                             To="8" Duration="0:0:0.2"/>
                                        </Storyboard>
                                    </BeginStoryboard>
                                </Trigger.ExitActions>
                            </Trigger>
                            <Trigger Property="IsDragging" Value="True">
                                <Setter TargetName="rectangle" Property="Fill" Value="#357ABD"/>
                                <Setter TargetName="rectangle" Property="Opacity" Value="1.0"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="AestheticScrollBar" TargetType="{x:Type ScrollBar}">
            <Setter Property="SnapsToDevicePixels" Value="True"/>
            <Setter Property="OverridesDefaultStyle" Value="true"/>
            <Style.Triggers>
                <Trigger Property="Orientation" Value="Vertical">
                    <Setter Property="Width" Value="1"/>
                    <Setter Property="Height" Value="Auto"/>
                    <Setter Property="Template">
                        <Setter.Value>
                            <ControlTemplate TargetType="{x:Type ScrollBar}">
                                <Grid Background="Transparent" Margin="0,0,2,0">
                                    <Track x:Name="PART_Track"
                                           IsDirectionReversed="true"
                                           HorizontalAlignment="Right"
                                           Width="10">
                                        <Track.DecreaseRepeatButton>
                                            <RepeatButton Style="{x:Null}" 
                                                          Background="Transparent" 
                                                          Focusable="false" 
                                                          IsTabStop="false" 
                                                          Command="ScrollBar.PageUpCommand"/>
                                        </Track.DecreaseRepeatButton>
                                        <Track.Thumb>
                                            <Thumb Style="{StaticResource ModernScrollBarThumb}"/>
                                        </Track.Thumb>
                                        <Track.IncreaseRepeatButton>
                                            <RepeatButton Style="{x:Null}" 
                                                          Background="Transparent" 
                                                          Focusable="false" 
                                                          IsTabStop="false" 
                                                          Command="ScrollBar.PageDownCommand"/>
                                        </Track.IncreaseRepeatButton>
                                    </Track>
                                </Grid>
                            </ControlTemplate>
                        </Setter.Value>
                    </Setter>
                </Trigger>
            </Style.Triggers>
        </Style>

        <Style x:Key="AestheticScrollViewer" TargetType="{x:Type ScrollViewer}">
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="{x:Type ScrollViewer}">
                        <Grid x:Name="Grid" Background="{TemplateBinding Background}">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="Auto"/>
                            </Grid.ColumnDefinitions>
                            <Grid.RowDefinitions>
                                <RowDefinition Height="*"/>
                                <RowDefinition Height="Auto"/>
                            </Grid.RowDefinitions>

                            <ScrollContentPresenter x:Name="PART_ScrollContentPresenter" 
                                                    Grid.Column="0" Grid.Row="0"
                                                    Margin="{TemplateBinding Padding}" 
                                                    Content="{TemplateBinding Content}" 
                                                    ContentTemplate="{TemplateBinding ContentTemplate}" 
                                                    CanContentScroll="{TemplateBinding CanContentScroll}" 
                                                    CanHorizontallyScroll="False" 
                                                    CanVerticallyScroll="False"/>

                            <ScrollBar x:Name="PART_VerticalScrollBar" 
                                       Grid.Column="1" Grid.Row="0"
                                       Value="{TemplateBinding VerticalOffset}" 
                                       Maximum="{TemplateBinding ScrollableHeight}" 
                                       ViewportSize="{TemplateBinding ViewportHeight}" 
                                       Visibility="{TemplateBinding ComputedVerticalScrollBarVisibility}"
                                       Style="{StaticResource AestheticScrollBar}"/>

                            <ScrollBar x:Name="PART_HorizontalScrollBar" 
                                       Grid.Column="0" Grid.Row="1"
                                       Orientation="Horizontal" 
                                       Value="{TemplateBinding HorizontalOffset}" 
                                       Maximum="{TemplateBinding ScrollableWidth}" 
                                       ViewportSize="{TemplateBinding ViewportWidth}" 
                                       Visibility="{TemplateBinding ComputedHorizontalScrollBarVisibility}"
                                       Style="{StaticResource AestheticScrollBar}"/>
                        </Grid>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </UserControl.Resources>

    <Border Background="White"
            CornerRadius="12"
            Padding="30"
            materialDesign:ShadowAssist.ShadowDepth="Depth2"
            Width="500"
            HorizontalAlignment="Center"
            VerticalAlignment="Center">

        <ScrollViewer VerticalScrollBarVisibility="Auto" 
                      Style="{StaticResource AestheticScrollViewer}">

            <StackPanel Orientation="Vertical">
                <StackPanel Orientation="Vertical" Margin="0,0,0,15">
                    <!-- Text Fields -->
                    <StackPanel Margin="10">
                        <Label Content="Last Name*" Margin="0,0,0,5" FontWeight="Medium" Foreground="#374151" FontSize="13"/>
                        <TextBox x:Name="LastNameTextBox" Style="{StaticResource RoundedTextBoxStyle}" Height="63"/>
                    </StackPanel>

                    <StackPanel Margin="10">
                        <Label Content="First Name*" Margin="0,0,0,5" FontWeight="Medium" Foreground="#374151" FontSize="13"/>
                        <TextBox x:Name="FirstNameTextBox" Style="{StaticResource RoundedTextBoxStyle}" Height="63"/>
                    </StackPanel>

                    <StackPanel Margin="10">
                        <Label Content="Middle Name*" Margin="0,0,0,5" FontWeight="Medium" Foreground="#374151" FontSize="13"/>
                        <TextBox x:Name="MiddleNameTextBox" Style="{StaticResource RoundedTextBoxStyle}" Height="63"/>
                    </StackPanel>

                    <StackPanel Margin="10">
                        <Label Content="Extension Name" Margin="0,0,0,5" FontWeight="Medium" Foreground="#374151" FontSize="13"/>
                        <ComboBox x:Name="ExtensionComboBox">
                            <ComboBoxItem Content="Jr."/>
                            <ComboBoxItem Content="Sr."/>
                            <ComboBoxItem Content="I."/>
                            <ComboBoxItem Content="II."/>
                            <ComboBoxItem Content="III."/>
                        </ComboBox>
                    </StackPanel>

                    <StackPanel Margin="10">
                        <Label Content="Gender*" Margin="0,0,0,5" FontWeight="Medium" Foreground="#374151" FontSize="13"/>
                        <ComboBox x:Name="GenderComboBox">
                            <ComboBoxItem Content="Male"/>
                            <ComboBoxItem Content="Female"/>
                        </ComboBox>
                    </StackPanel>

                    <StackPanel Margin="10">
                        <Label Content="Birthday*" Margin="0,0,0,5" FontWeight="Medium" Foreground="#374151" FontSize="13"/>
                        <DatePicker x:Name="BirthdayDatePicker"/>
                    </StackPanel>

                    <StackPanel Margin="10">
                        <Label Content="Contact Number" Margin="0,0,0,5" FontWeight="Medium" Foreground="#374151" FontSize="13"/>
                        <TextBox x:Name="ContactNumberTextBox" Style="{StaticResource RoundedTextBoxStyle}" Height="63"/>
                    </StackPanel>

                    <StackPanel Margin="10">
                        <Label Content="Barangay" Margin="0,0,0,5" FontWeight="Medium" Foreground="#374151" FontSize="13"/>
                        <ComboBox x:Name="BarangayComboBox">
                            <ComboBoxItem Content="Dela Paz"/>
                            <ComboBoxItem Content="Casile"/>
                        </ComboBox>
                    </StackPanel>

                    <StackPanel Margin="10">
                        <Label Content="Address" Margin="0,0,0,5" FontWeight="Medium" Foreground="#374151" FontSize="13"/>
                        <TextBox x:Name="AddressTextBox" Style="{StaticResource RoundedTextBoxStyle}" Height="63"/>
                    </StackPanel>

                    <StackPanel Margin="10">
                        <Label Content="Criminal Case" Margin="0,0,0,5" FontWeight="Medium" Foreground="#374151" FontSize="13"/>
                        <TextBox x:Name="CriminalCaseTextBox" Style="{StaticResource RoundedTextBoxStyle}" Height="63"/>
                    </StackPanel>

                    <StackPanel Margin="10">
                        <Label Content="Offense Committed" Margin="0,0,0,5" FontWeight="Medium" Foreground="#374151" FontSize="13"/>
                        <TextBox x:Name="OffenseCommittedTextBox" Style="{StaticResource RoundedTextBoxStyle}" Height="63"/>
                    </StackPanel>

                    <StackPanel Margin="10">
                        <Label Content="Description" Margin="0,0,0,5" FontWeight="Medium" Foreground="#374151" FontSize="13"/>
                        <TextBox x:Name="DescriptionTextBox" Style="{StaticResource RoundedTextBoxStyle}" Height="63"/>
                    </StackPanel>

                    <StackPanel Margin="10">
                        <Label Content="Court Number" Margin="0,0,0,5" FontWeight="Medium" Foreground="#374151" FontSize="13"/>
                        <TextBox x:Name="CourtNumberTextBox" Style="{StaticResource RoundedTextBoxStyle}" Height="63"/>
                    </StackPanel>

                    <StackPanel Margin="10">
                        <Label Content="Committing Court" Margin="0,0,0,5" FontWeight="Medium" Foreground="#374151" FontSize="13"/>
                        <TextBox x:Name="CommittingCourtTextBox" Style="{StaticResource RoundedTextBoxStyle}" Height="63"/>
                    </StackPanel>

                    <StackPanel Margin="10">
                        <Label Content="Date Admitted" Margin="0,0,0,5" FontWeight="Medium" Foreground="#374151" FontSize="13"/>
                        <DatePicker x:Name="DateAdmittedDatePicker"/>
                    </StackPanel>

                    <StackPanel Margin="10">
                        <Label Content="Status of Case" Margin="0,0,0,5" FontWeight="Medium" Foreground="#374151" FontSize="13"/>
                        <ComboBox x:Name="StatusOfCaseComboBox">
                            <ComboBoxItem Content="Ongoing"/>
                            <ComboBoxItem Content="Completed"/>
                            <ComboBoxItem Content="Dropped"/>
                        </ComboBox>
                    </StackPanel>

                    <StackPanel Margin="10">
                        <Label Content="Date Graduated" Margin="0,0,0,5" FontWeight="Medium" Foreground="#374151" FontSize="13"/>
                        <DatePicker x:Name="DateGraduatedDatePicker"/>
                    </StackPanel>

                    <StackPanel Margin="10">
                        <Label Content="Remarks" Margin="0,0,0,5" FontWeight="Medium" Foreground="#374151" FontSize="13"/>
                        <TextBox x:Name="RemarksTextBox" Style="{StaticResource RoundedTextBoxStyle}" Height="63"/>
                    </StackPanel>
                </StackPanel>

                <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" Margin="10,20,0,0">
                    <Button Content="Cancel"
                            Style="{StaticResource MaterialDesignFlatButton}"
                            Margin="0,0,10,0"
                            Foreground="#10B981"
                            Click="Cancel_Click"/>
                    <Button Content="Save"
                            Style="{StaticResource MaterialDesignRaisedButton}"
                            Background="#10B981"
                            Foreground="White"
                            Click="Save_Click"/>
                </StackPanel>
            </StackPanel>
        </ScrollViewer>
    </Border>
</UserControl>
